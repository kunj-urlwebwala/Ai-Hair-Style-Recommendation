// Storage keys for the local session. Kept separate from API config so the
// values stay stable across releases.
export const SESSION_TOKEN_KEY = "app_session_token";
export const USER_INFO_KEY = "app_user_info";
